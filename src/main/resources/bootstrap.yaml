spring:
  application:
    name: currency-exchange-service
  cloud:
    config:
      uri: http://localhost:8888
  jpa:
    show-sql: true
  h2:
    console:
      enable: true
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

server:
  port: 8000
